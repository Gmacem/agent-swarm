# Run Tests

Run the project's test suite and report results.

## Arguments

- `$ARGUMENTS` — Optional: specific test file or pattern to run (e.g. "tests/auth/" or "test_users.py")

## Instructions

1. Check `prompts/project.md` for the test framework and run command.
2. Run the test suite (or the specified subset).
3. Report results:
   - Total tests, passed, failed, skipped.
   - For failures: test name, error message, relevant file/line.
4. If tests fail, summarize what's broken and suggest fixes.
5. Update your task with the test results.
